import {FormattedText} from '~defined-types/text-formatting.type';

const text =
  '<n>Hello, my name is <h1><b>Chinh<n>. You would be <i>nice<n> to <cr>meet<n> me.';

export const returnFormattedTextObject = (
  inputFormattedText: FormattedText,
) => {};
